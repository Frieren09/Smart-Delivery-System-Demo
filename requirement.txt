# 1. Create the toolbox (creates a folder named 'venv')
python -m venv venv

# 2. Activate the toolbox (your prompt will change to show "(venv)")
.\venv\Scripts\Activate.ps1

# 3. Install Flask and the database connector into the toolbox
pip install Flask psycopg2-binary
